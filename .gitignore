Nothing should be ignored in this case, as the changes only include source code files (.xaml and .xaml.cs) which are part of the normal project files and not build artifacts, dependencies, or temporary files.